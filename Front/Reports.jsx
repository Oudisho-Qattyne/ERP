// ════════════ REPORTS ════════════
function ReportsView() {
  const secD=useMemo(()=>{const s={};DB.investors.filter(i=>i.status==="active").forEach(i=>{s[i.sector]=(s[i.sector]||0)+1});return Object.entries(s).map(([k,v],i)=>({name:k,value:v,fill:CC[i%CC.length]}));},[]);
  const plD=useMemo(()=>{const z=[...new Set(DB.plots.map(p=>p.zone))];return z.map(z=>{const zp=DB.plots.filter(p=>p.zone===z);return{name:z,مخصص:zp.filter(p=>["allocated","reserved"].includes(p.status)).length,متاح:zp.filter(p=>p.status==="available").length};});},[]);
  return <div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:14,marginBottom:18}}><div style={cd}><h4 style={{fontSize:13,fontWeight:700,margin:"0 0 12px"}}>الإيرادات والمصروفات (ل.س)</h4><ResponsiveContainer width="100%" height={200}><AreaChart data={DB.monthly}><CartesianGrid strokeDasharray="3 3" stroke="#dce3dc"/><XAxis dataKey="m" tick={{fontSize:10}}/><YAxis tick={{fontSize:9}} tickFormatter={v=>fmtSYP(v)}/><Tooltip formatter={v=>v.toLocaleString()+" ل.س"}/><Legend/><Area type="monotone" dataKey="rev" stroke={C.green} fill={C.green} fillOpacity={0.1} strokeWidth={2} name="الإيرادات"/><Area type="monotone" dataKey="exp" stroke={C.warning} fill={C.warning} fillOpacity={0.05} strokeWidth={2} name="المصروفات"/></AreaChart></ResponsiveContainer></div><div style={cd}><h4 style={{fontSize:13,fontWeight:700,margin:"0 0 12px"}}>المستثمرون حسب القطاع</h4><ResponsiveContainer width="100%" height={200}><PieChart><Pie data={secD} cx="50%" cy="50%" innerRadius={35} outerRadius={70} dataKey="value" label={({name,value})=>name+" ("+value+")"}>{secD.map((e,i)=><Cell key={i} fill={e.fill}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div><div style={cd}><h4 style={{fontSize:13,fontWeight:700,margin:"0 0 12px"}}>إشغال الأراضي</h4><ResponsiveContainer width="100%" height={200}><BarChart data={plD}><CartesianGrid strokeDasharray="3 3" stroke="#dce3dc"/><XAxis dataKey="name" tick={{fontSize:9}}/><YAxis tick={{fontSize:10}}/><Tooltip/><Legend/><Bar dataKey="مخصص" fill={C.green} radius={[4,4,0,0]}/><Bar dataKey="متاح" fill={C.gold} radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div><div style={cd}><h4 style={{fontSize:13,fontWeight:700,margin:"0 0 12px"}}>نسبة الإشغال الشهرية</h4><ResponsiveContainer width="100%" height={200}><AreaChart data={DB.monthly}><CartesianGrid strokeDasharray="3 3" stroke="#dce3dc"/><XAxis dataKey="m" tick={{fontSize:10}}/><YAxis tick={{fontSize:10}} domain={[60,100]} tickFormatter={v=>v+"%"}/><Tooltip formatter={v=>v+"%"}/><Area type="monotone" dataKey="occ" stroke={C.gold} fill={C.gold} fillOpacity={0.15} strokeWidth={2.5} name="الإشغال"/></AreaChart></ResponsiveContainer></div></div></div>;
}

